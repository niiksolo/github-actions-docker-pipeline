name: CD Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - develop
      - main

jobs:
  build:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker image
        run: docker build -t ghcr.io/${{ github.repository }}:${{ github.sha }} .

      - name: Push Docker image (commit tag)
        run: docker push ghcr.io/${{ github.repository }}:${{ github.sha }}

      - name: Add branch tags
        if: github.ref_name == 'develop' || github.ref_name == 'main'
        run: |
          if [ "${{ github.ref_name }}" = "develop" ]; then
            docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:staging
            docker push ghcr.io/${{ github.repository }}:staging
          fi
          if [ "${{ github.ref_name }}" = "main" ]; then
            docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:latest
            docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:prod
            docker push ghcr.io/${{ github.repository }}:latest
            docker push ghcr.io/${{ github.repository }}:prod
          fi

  deploy_staging:
    name: 🚀 Deploy Staging
    runs-on: ubuntu-latest
    needs: build
    if: github.ref_name == 'develop' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Deploy staging
        run: |
          echo "🚀 Deploy to staging (using image ghcr.io/${{ github.repository }}:staging)"
          # Здесь подключается docker-compose/kubectl/ssh для staging

  deploy_prod:
    name: 🚀 Deploy Production
    runs-on: ubuntu-latest
    needs: build
    if: github.ref_name == 'main'
    steps:
      - name: Deploy production
        run: |
          echo "🚀 Deploy to production (using image ghcr.io/${{ github.repository }}:prod)"
          # Здесь подключается docker-compose/kubectl/ssh для prod