name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]   # Ждём успешного CI
    types:
      - completed
  workflow_dispatch:             # Можно вручную запустить staging

jobs:
  build:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build Docker image
        run: docker build -t ghcr.io/${{ github.repository }}/app:${{ github.sha }} .

      - name: Push Docker image (commit tag)
        run: docker push ghcr.io/${{ github.repository }}/app:${{ github.sha }}

      - name: Add branch tags
        run: |
          if [ "${{ github.ref_name }}" = "develop" ]; then
            docker tag ghcr.io/${{ github.repository }}/app:${{ github.sha }} ghcr.io/${{ github.repository }}/app:staging
            docker push ghcr.io/${{ github.repository }}/app:staging
          fi
          if [ "${{ github.ref_name }}" = "main" ]; then
            docker tag ghcr.io/${{ github.repository }}/app:${{ github.sha }} ghcr.io/${{ github.repository }}/app:latest
            docker tag ghcr.io/${{ github.repository }}/app:${{ github.sha }} ghcr.io/${{ github.repository }}/app:prod
            docker push ghcr.io/${{ github.repository }}/app:latest
            docker push ghcr.io/${{ github.repository }}/app:prod
          fi

  deploy_staging:
    name: 🚀 Deploy Staging
    runs-on: ubuntu-latest
    needs: build
    if: github.ref_name == 'develop' || github.event_name == 'workflow_dispatch'
    steps:
      - name: Deploy staging
        run: |
          echo "🚀 Deploy to staging (using image ghcr.io/${{ github.repository }}/app:staging)"
          # docker-compose/kubectl/ssh для staging

  deploy_prod:
    name: 🚀 Deploy Production
    runs-on: ubuntu-latest
    needs: build
    if: github.ref_name == 'main'
    steps:
      - name: Deploy production
        run: |
          echo "🚀 Deploy to production (using image ghcr.io/${{ github.repository }}/app:prod)"
          # docker-compose/kubectl/ssh для prod