name: CD Pipeline

on:
  workflow_run:
    workflows: ["CI Pipeline"]
    types:
      - completed

jobs:
  build:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin


      - name: Build Docker image
        run: docker build -t ghcr.io/${{ github.repository }}:${{ github.sha }} .


      - name: Push Docker image (commit tag)
        run: docker push ghcr.io/${{ github.repository }}:${{ github.sha }}


      - name: Add branch tags
        if: github.event.workflow_run.head_branch == 'develop' || github.event.workflow_run.head_branch == 'main'
        run: |
          if [ "${{ github.event.workflow_run.head_branch }}" = "develop" ]; then
            docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:staging
            docker push ghcr.io/${{ github.repository }}:staging
          fi
          if [ "${{ github.event.workflow_run.head_branch }}" = "main" ]; then
            docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:latest
            docker tag ghcr.io/${{ github.repository }}:${{ github.sha }} ghcr.io/${{ github.repository }}:prod
            docker push ghcr.io/${{ github.repository }}:latest
            docker push ghcr.io/${{ github.repository }}:prod
          fi
       

  deploy_staging:
    name: 🚀 Deploy Staging
    runs-on: ubuntu-latest
    needs: build
    if: github.event.workflow_run.head_branch == 'develop'
    steps:
      - name: Deploy staging
        run: |
          echo "🚀 Deploy to staging (image: ghcr.io/${{ github.repository }}:staging)"
          # здесь может быть docker-compose/kubectl/ssh для реального деплоя

  deploy_prod:
    name: 🚀 Deploy Production
    runs-on: ubuntu-latest
    needs: build
    if: github.event.workflow_run.head_branch == 'main'
    steps:
      - name: Deploy production
        run: |
          echo "🚀 Deploy to production (image: ghcr.io/${{ github.repository }}:prod)"
          # здесь docker-compose/kubectl/ssh для prod